// https://pris.ly/d/prisma-schema

generator client {
    provider        = "prisma-client-js"
    previewFeatures = ["strictUndefinedChecks"]
}

generator erd {
    provider = "prisma-erd-generator"
    output   = "schema_db.png"
}

datasource db {
    provider = "postgresql"
}