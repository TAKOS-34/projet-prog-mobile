model UserFollow {
    followerId  Int
    followingId Int
    follower    User @relation("UserFollowsGiven", fields: [followerId], references: [id], onDelete: Cascade)
    following   User @relation("UserFollowsReceived", fields: [followingId], references: [id], onDelete: Cascade)

    @@id([followerId, followingId])
}



model GroupFollow {
    followerId  Int
    groupId     Int
    follower    User @relation(fields: [followerId], references: [id], onDelete: Cascade)
    following   Group @relation(fields: [groupId], references: [id], onDelete: Cascade)

    @@id([followerId, groupId])
}



model TagFollow {
    followerId  Int
    tagId       Int
    follower    User @relation(fields: [followerId], references: [id], onDelete: Cascade)
    following   Tag  @relation(fields: [tagId], references: [id], onDelete: Cascade)

    @@id([followerId, tagId])
}